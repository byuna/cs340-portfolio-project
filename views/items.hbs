<H1>Items</H1>

{{!-- Create a table --}}
<table id="items-table">
  {{!-- Header section --}}
  <thead>
    {{!-- For just the first row, we print each key of the row object as a header cell so we
      know what each column means when the page renders --}}
    <tr>
      {{#each data.[0]}}
      <th>
        {{@key}}
      </th>
      {{/each}}
      <th>Delete</th>
    </tr>
  </thead>

  
  {{!-- Body section --}}
  <tbody>
    {{#each data}}
    <tr>
      <td>{{"Item ID"}}</td>
      <td>{{"Description"}}</td>
      <td>{{"Cost"}}</td>
      <td>{{"Format"}}</td>
      <td>{{"Purpose"}}</td>
      <td><button onclick="deleteItem()">Delete</button></td>
    </tr>
    {{/each}}
  </tbody>
</table>

<h2>New Item Addition</h2>
<p>To add a new item to the inventory, please enter the information below and click 'Submit'!</p>
<form id="add-item-form-ajax">
  <label for="input-item_description">Item Description: </label>
  <input type="text" name="input-item_description" id="input-item_description">

  <label for="input-item_cost">Item Cost: </label>
  <input type="number" name="input-item_cost" id="input-item_cost">

  <label for="input-pc_format">PC Format: </label>
  <input type="text" name="input-pc_format" id="input-pc_format">

  <label for="input-pc_purpose">PC Purpose: </label>
  <input type="text" name="input-pc_purpose" id="input-pc_purpose">

  <input type="submit">
</form>

<h2>Filter Items By Types</h2>
  <p>Use the available drop downs to filter for a certain type of PC!</p>
<form>
  <select name="item-format-filter" id="item-format-filter-ajax">
    <option value="">Filter Items by Format</option>
    <option value="Desktop">Desktop</option>
    <option value="Laptop">Laptop</option>
</select>

<select name="item-purpose-filter" id="item-purpose-filter-ajax">
    <option value="">Filter Items by Purpose</option>
    <option value="Gaming">Gaming</option>
    <option value="Business">Business</option>
    <option value="Home">Home</option>
</select>
<input type="submit">
</form>

{{!-- Embed our javascript to handle the DOM manipulation and AJAX request --}}
<script src="./js/add_item.js"></script>